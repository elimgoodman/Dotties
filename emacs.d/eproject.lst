;; -*- mode: Lisp; -*-

(setq prj-version nil)

(setq prj-list
      '(("blarg" "~/dev/blarg")
        ("eproject" "/Users/eli/.emacs.d/eproject")))

(setq prj-last-open nil)

(setq prj-frame-pos
      '((top . 135)
        (left . 250)
        (width . 158)
        (height . 41)))

