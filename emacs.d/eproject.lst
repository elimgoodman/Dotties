;; -*- mode: Lisp; -*-

(setq prj-version nil)

(setq prj-list
      '(("blarg" "~/dev/blarg")
        ("eproject" "/Users/eli/.emacs.d/eproject")))

(setq prj-last-open nil)

(setq prj-frame-pos
      '((top . 22)
        (left . 0)
        (width . 238)
        (height . 57)))

